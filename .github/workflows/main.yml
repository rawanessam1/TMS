name: CI/CD Pipeline

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js for Backend
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: "pern-mcp-starter/backend/package-lock.json"

      - name: Install Backend Dependencies
        working-directory: ./pern-mcp-starter/backend
        run: npm ci

      - name: Set up Node.js for Frontend
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: "pern-mcp-starter/frontend/package-lock.json"

      - name: Install Frontend Dependencies
        working-directory: ./pern-mcp-starter/frontend
        run: npm ci

      - name: Lint Frontend
        working-directory: ./pern-mcp-starter/frontend
        run: npm run lint

      - name: Build Frontend
        working-directory: ./pern-mcp-starter/frontend
        run: npm run build

      - name: Validate docker-compose syntax
        working-directory: ./pern-mcp-starter
        run: docker compose config -q # ✅ no hyphen
